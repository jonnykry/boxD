##Simple websocket server test (for now).

**running the test server**

- From the server directory, run `pip install -r requirements.txt`

- Run the server with `python server.py`

- Open index.html in a web browser